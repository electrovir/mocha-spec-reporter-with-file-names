{
    "name": "manual-test",
    "private": true,
    "scripts": {
        "test": "ts-node ./src/setup-test.ts && mocha --config .mocharc.js --sort --parallel=false"
    },
    "dependencies": {
        "mocha-spec-reporter-with-file-names": "file:../../packed.tgz"
    }
}
